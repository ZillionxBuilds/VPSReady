# Project-scoped Codex defaults. Loaded only when this repository is trusted.
# Current repository migration is SOLO; do not start historical orchestration.
# Role definitions remain historical/reference-only unless Owner explicitly reauthorizes.

model = "gpt-5.6-terra"
model_reasoning_effort = "high"

[agents]
enabled = false
max_concurrent_threads_per_session = 1
interrupt_message = true
